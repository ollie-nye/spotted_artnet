pub mod art_header;
pub mod opcode;

pub const IMAGE_WIDTH: usize = 256;
pub const IMAGE_HEIGHT: usize = 160;
pub const IMAGE_SIZE: usize = (IMAGE_HEIGHT * IMAGE_WIDTH);
pub const RGB_IMAGE_SIZE: usize = (IMAGE_SIZE * 3);
